build:
  environment:
    docker_compose:
    create_db:
  codebase:
    assemble:
      checkout:
      - repo:
        branch:
        depth:
        checkout_dir:
        commit_hash:
      - repo:
        branch:
        depth:
        checkout_dir:
        commit_hash:
      patch:
      - file:
        apply_dir:
      fetch:
        - url:
          directory:
        - url:
          directory:
      composer:
        - command:
          options:
          data:
    validate:
      phplint: True
  assessment:
    - simpletest:
        testgroups:
        runtests:
          xml: %DCI_RTXmlPath%
          color: FALSE
        xmlformat:
    - simpletest:
        testgroups:
        runtests:
          options:
          arguments:
        xmlformat:



